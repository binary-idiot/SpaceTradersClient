@using SpaceTraders.Features.AgentFeature.State
@using Fluxor
@inject IState<AgentState> AccountState

<div>
    <div>Account ID: @AccountState.Value?.Agent?.AccountId</div>
    <div>Agent: @AccountState.Value?.Agent?.Symbol</div>
    <div>Headquarters: @AccountState.Value?.Agent?.Headquarters</div>
    <div>Credits: @AccountState.Value?.Agent?.Credits</div>
    <div>Faction: @AccountState.Value?.Agent?.StartingFaction</div>
    <div>Ship Count: @AccountState.Value?.Agent?.ShipCount</div>
</div>

@code {
    
}